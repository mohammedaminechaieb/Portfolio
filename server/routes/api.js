// All API routes are handled directly in server.js via Express app.
// This file is reserved for future route modularization.
// No in-memory data — everything goes through MongoDB via Mongoose models.
import express from 'express';
const router = express.Router();
export default router;
